{
  "name": "TDD Node",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "test:unit": "yarn jest",
    "lint": "eslint . --ext .ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^28.1.0",
    "nodemon": "^2.0.16",
    "supertest": "^6.2.3"
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "build/"
    ]
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "jest": "^28.1.0",
    "live-server": "^1.2.2"
  }
}
